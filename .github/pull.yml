version: "1"
rules:                      # Array of rules
  - base: powernukkit/own          # Required. Target branch
    upstream: PowerNukkit:bleeding # Required. Must be in the same fork network.
    mergeMethod: none       # Optional, one of [none, merge, squash, rebase, hardreset], Default: none.
    mergeUnstable: false    # Optional, merge pull request even when the mergeable_state is not clean. Default: false
    assignees:              # Optional
      - KCodeYT
    reviewers:              # Optional
      - KCodeYT
    conflictReviewers:      # Optional, on merge conflict assign a reviewer
      - KCodeYT
label: "Status: Awaiting Review"  # Optional
